Notes:

With quatro:

1. Install quatro
2. quarto render skipgram_theory_practice.ipynb --to html
3. Copy files (.html and sub-directory generated) to the saibaba.github.io git repo directory
4. Commit and push (make sure auth token is configured)
